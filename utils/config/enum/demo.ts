export const demoStr = Object.freeze({
  1: '今日',
  2: '明日'
});

export const demoType = Object.freeze({
  today: 1,
  tomorrow: 2
});

export const demoKey = Object.freeze({
  1: 'today',
  2: 'tomorrow'
});
